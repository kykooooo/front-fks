# engines.node = "22" épingle la runtime de déploiement (aussi dans firebase.json).
# En local (dev sur Node >22), on n'échoue pas sur la vérif de version : la runtime
# réellement déployée reste nodejs22. Sans effet en CI (déjà Node 22).
--ignore-engines true
